const friend = "BRUTUS";
const shiftValue = 3;
const alphabet = "abcdefghijklmnnopqrstuvwxyz";
const firstLetter = friend[0];
const index = alphabet.indexOf(firstLetter.toLowerCase());
// Because we use an idex of 0 to start and not a 1.
const newIndex = index + shiftValue;
const encryptedFirstletter = alphabet[e]
// modulus 
alphabet.Length; 
27
const newIndex = (index + shiftValue) % alphabetLength;
const encryptedFirstletter = alphabet[newIndex]
const encryptedMessage = "EUXWXV";
encryptedMessage.slice(0, 3);